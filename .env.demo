# App
APP_ENV="demo"
NEXT_PUBLIC_APP_ENV="demo"

# Database
DATABASE_URL="postgresql://postgres:postgres@db:5432/arcana"

# PostgreSQL (docker-compose で使用)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=arcana
